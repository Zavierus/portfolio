# Runtime Assets

This directory contains optimized GLB models, KTX2/WebP textures, delivery audio, LODs, and fallback media generated from `assets/source/`.

Runtime files are validated against project manifests before production builds. Concept references and unoptimized source files do not belong here.
